% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_model.R
\name{fit_model}
\alias{fit_model}
\alias{fit_model.rw_subtyped}
\alias{fit_model.ps_subtyped}
\alias{fit_model.rw_multiple}
\alias{fit_model.ps_multiple}
\alias{fit_model.rw_single}
\alias{fit_model.ps_single}
\title{Generic Method for fitting model}
\usage{
fit_model(
  constructed_model,
  n_chain = 4,
  n_iter = 2000,
  n_warmup = floor(n_iter/2),
  thin = 1,
  adapt_delta = 0.9,
  multi_cores = TRUE,
  verbose = TRUE,
  suppress_warnings = FALSE,
  seed = NULL,
  ...
)

\method{fit_model}{rw_subtyped}(
  constructed_model,
  n_chain = 4,
  n_iter = 2000,
  n_warmup = floor(n_iter/2),
  thin = 1,
  adapt_delta = 0.9,
  multi_cores = TRUE,
  verbose = TRUE,
  suppress_warnings = FALSE,
  seed = NULL,
  ...
)

\method{fit_model}{ps_subtyped}(
  constructed_model,
  n_chain = 4,
  n_iter = 2000,
  n_warmup = floor(n_iter/2),
  thin = 1,
  adapt_delta = 0.9,
  multi_cores = TRUE,
  verbose = TRUE,
  suppress_warnings = FALSE,
  seed = NULL,
  ...
)

\method{fit_model}{rw_multiple}(
  constructed_model,
  n_chain = 4,
  n_iter = 2000,
  n_warmup = floor(n_iter/2),
  thin = 1,
  adapt_delta = 0.9,
  multi_cores = TRUE,
  verbose = TRUE,
  suppress_warnings = FALSE,
  seed = NULL,
  ...
)

\method{fit_model}{ps_multiple}(
  constructed_model,
  n_chain = 4,
  n_iter = 2000,
  n_warmup = floor(n_iter/2),
  thin = 1,
  adapt_delta = 0.9,
  multi_cores = TRUE,
  verbose = TRUE,
  suppress_warnings = FALSE,
  seed = NULL,
  ...
)

\method{fit_model}{rw_single}(
  constructed_model,
  n_chain = 4,
  n_iter = 2000,
  n_warmup = floor(n_iter/2),
  thin = 1,
  adapt_delta = 0.9,
  multi_cores = TRUE,
  verbose = TRUE,
  suppress_warnings = FALSE,
  seed = NULL,
  ...
)

\method{fit_model}{ps_single}(
  constructed_model,
  n_chain = 4,
  n_iter = 2000,
  n_warmup = floor(n_iter/2),
  thin = 1,
  adapt_delta = 0.9,
  multi_cores = TRUE,
  verbose = TRUE,
  suppress_warnings = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{constructed_model}{prepared model object of class
\code{EpiStrainDynamics.model}}

\item{n_chain}{number of MCMC chains, defaults to 4}

\item{n_iter}{A positive integer specifying the number of iterations for each chain, default value is  2000}

\item{n_warmup}{A positive integer specifying the number of warmup iterations, default value is half the number of iterations}

\item{thin}{A positive integer specifying the period for saving samples, default value is 1.}

\item{adapt_delta}{Numeric value between 0 and 1 indicating target average acceptance probability used in \code{rstan::sampling}. Default value is 0.9.}

\item{multi_cores}{A logical value indicating whether to parallelize chains with multiple cores, default is TRUE and uses all available cores - 1.}

\item{verbose}{Logical value controlling the verbosity of output. When TRUE (default),
shows all messages, warnings, errors, and progress indicators. When FALSE, suppresses
messages and progress while retaining warnings and errors.}

\item{suppress_warnings}{Logical value indicating whether to suppress warnings from Stan.
Default is FALSE. When TRUE, warnings are suppressed but errors are still raised.}

\item{seed}{A positive integer seed used for random number generation in MCMC. Default is NULL, which means the seed is generated from 1 to the maximum integer supported by R.}

\item{...}{additional arguments to \code{rstan::sampling()}, such as \code{init}}
}
\value{
fit model of class \code{EpiStrainDynamics.fit}, or if fitting fails,
an error is raised that can be caught and inspected.
}
\description{
S3 generic for fitted models from constructed model object
}
\examples{
\dontrun{
  mod <- construct_model(
    method = random_walk(),
    pathogen_structure = single(
      case_timeseries = sarscov2$cases,
      time = sarscov2$date))

  fit <- fit_model(mod)

  # Suppress progress and messages but keep warnings/errors
  fit <- fit_model(mod, verbose = FALSE)

  # Suppress warnings too
  fit <- fit_model(mod, verbose = FALSE, suppress_warnings = TRUE)

  # Catch errors and inspect
  result <- tryCatch(
    fit_model(mod),
    error = function(e) e
  )
  if (inherits(result, "EpiStrainDynamics.fit.error")) {
    cat("Fitting failed:", result$message, "\n")
    # Can still access the model: result$constructed_model
  }
}

}
